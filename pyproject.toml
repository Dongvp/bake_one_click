[build-system]
requires = ["setuptools>=61.0", "wheel", "cython", "numpy"]
build-backend = "setuptools.build_meta"

[project]
name = "bake_one_click_core"
version = "1.23.0"
description = "Compiled core module for Bake One Click Blender addon"
requires-python = ">=3.13"